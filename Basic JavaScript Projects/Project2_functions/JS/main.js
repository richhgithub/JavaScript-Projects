function myFunction() { //create a function
    var sentence = "I am learning"; //create var
    sentence += " a lot from this book!"; //concatenate a string using the += operator
    document.getElementById("Concatenate").innerHTML = sentence; //display the concatenated string
}
